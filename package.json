{
  "name": "spacebake",
  "version": "1.0.0",
  "private": true,
  "description": "SpaceBake — a relaxing idle space career sim (static, vanilla JS).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
